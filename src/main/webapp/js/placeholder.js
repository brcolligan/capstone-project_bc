// Git will not save empty directories, so this file is a placeholder to create the directory structure until the directory has contents.
var options = {
	valueNames: [ 'toolName', 'toolCategory', 'toolClientId', 'toolLoanPeriod' ]
	};
	var userList = new List('users', options);